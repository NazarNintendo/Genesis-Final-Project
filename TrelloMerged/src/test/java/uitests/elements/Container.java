package uitests.elements;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class Container extends Element {
    public Container(WebDriver driver, By by) {
        super(driver, by);
    }
}
